from __future__ import annotations
import numpy as np

def sample_uniform(n: int, dim: int, *, rng: np.random.Generator) -> np.ndarray:
    """Uniform points in [0,1]^dim, shape (n, dim)."""
    return rng.random((n, dim), dtype=float)
